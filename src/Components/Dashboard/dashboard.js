import React from 'react';

function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>This is a protected route, visible only to logged-in users.</p>
    </div>
  );
}

export default Dashboard;